import { CoursePageLayout } from "@/components/course-page-layout";

export default function AdvancedCardiovascularLifeSupportLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <CoursePageLayout>{children}</CoursePageLayout>;
}
