import { LayoutGrid } from "lucide-react";
import { BlankPlaceholder } from "@/components/patterns/client-templates-migrate/shared";

export default function AllClassesRoute() {
  return <BlankPlaceholder title="All Classes" icon={<LayoutGrid size={16} strokeWidth={1.75} />} />;
}
