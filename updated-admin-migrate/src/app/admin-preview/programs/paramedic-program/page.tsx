import { GraduationCap } from "lucide-react";
import { BlankPlaceholder } from "@/components/patterns/client-templates-migrate/shared";

export default function ParamedicProgramRoute() {
  return (
    <BlankPlaceholder
      title="Paramedic Program"
      icon={<GraduationCap size={16} strokeWidth={1.75} />}
    />
  );
}
